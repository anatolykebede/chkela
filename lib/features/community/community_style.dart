import '../../core/theme/app_colors.dart';

/// Community feed aliases — kept so the feature stays readable.
class CommunityColors {
  CommunityColors._();

  static const bg = AppColors.bgBase;
  static const surface = AppColors.bgSurface;
  static const border = AppColors.border;
  static const textPrimary = AppColors.textPrimary;
  static const textSecondary = AppColors.textSecondary;
  static const verified = AppColors.verified;
  static const fabPink = AppColors.fabPink;
  static const pillBg = AppColors.bgSurface;
  static const iconMuted = AppColors.iconMuted;
}
