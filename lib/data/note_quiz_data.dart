import 'exam_questions.dart';

/// Legacy note-quiz helper. Quizzes come from the CMS only.
List<ExamQuestion> questionsForNote({
  required String noteId,
  required String noteTitle,
  required String subjectName,
}) {
  return const [];
}
